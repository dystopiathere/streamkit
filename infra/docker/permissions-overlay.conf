# Оверлей ничего не публикует: он только показывает гостей и проигрывает их
# звук. Камера и микрофон ему не нужны — значит, закрыты.
add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), interest-cohort=()" always;
