# Дашборду камера и микрофон нужны: из него стример и гость входят в
# приватную комнату. Только своему origin — встроенная чужая страница их не получит.
add_header Permissions-Policy "camera=(self), microphone=(self), geolocation=(), interest-cohort=()" always;
