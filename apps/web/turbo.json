{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/apps/admin/index.html",
        "$TURBO_ROOT$/apps/overlay/index.html"
      ]
    }
  }
}
