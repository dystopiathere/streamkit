-- Тариф, выбранный на СЛЕДУЮЩИЙ период.
--
-- Отдельное поле, а не правка `plan`: смена тарифа применяется при продлении, и
-- до конца оплаченного периода доступ обязан остаться прежним (оферта, 2.5).
-- Записывая выбор прямо в `plan`, переход с «Про» на «Мультистрим» закрывал бы
-- приватные комнаты в тот же миг — за уже оплаченные дни.
--
-- NULL означает «сменить не просили»: продлится тот же тариф.
ALTER TABLE "Subscription" ADD COLUMN "nextPlan" "Plan";
