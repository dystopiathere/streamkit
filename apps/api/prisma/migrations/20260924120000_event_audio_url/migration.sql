-- Голосовой донат: ссылка на запись у провайдера (DonationAlerts).
-- Файл остаётся у него: мы храним адрес и проигрываем его в кадре, поэтому
-- запись исчезает вместе с событием при обнулении истории и удалении аккаунта.
ALTER TABLE "AlertEvent" ADD COLUMN "audioUrl" VARCHAR(2048);
